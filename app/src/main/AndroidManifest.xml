<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="in.codewithmike.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".UserInput.CourtCounter"></activity>
        <activity android:name=".UserInput.UserInputMain" />
        <activity android:name=".UserInput.JustJavaApp" />
        <activity android:name=".UserInterface.SelfGreetingCard" />
        <activity android:name=".UserInterface.ProjectRubric" />
        <activity android:name=".UserInterface.BirthdayGreetingApp" />
        <activity android:name=".UserInterface.UserInterfaceMain" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>